/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['github.com', 'firebasestorage.googleapis.com', 'm.media-amazon.com']
    }
}

module.exports = nextConfig
